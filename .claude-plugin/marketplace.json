{
  "name": "lean-output",
  "owner": {
    "name": "wasdevv"
  },
  "plugins": [
    {
      "name": "lean-output",
      "source": "./",
      "description": "Spills long tool results to disk and hands back a pointer, withholds results the context already holds, compresses RSpec, RuboCop, Brakeman, git diff, cargo and grep output before it reaches the model, and at a compaction drops the tool calls a later identical call already answered — fewer tokens, zero lost failures, nothing sent anywhere."
    }
  ]
}
